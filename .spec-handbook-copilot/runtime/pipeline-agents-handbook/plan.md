# Plan — pipeline-agents-handbook

## Sujet (verbatim utilisateur)

> On va construire dans ressources une explication de comment on se sert des agents pour le site. A quoi ils servent etc ... c'est à la fois une page ou un ensemble de page permettant a chacun de contribuer mais aussi un vrai exemple de workflow permettant de comprendre ce qu'on a vu dans la partie workflow.
> Utilise Genesis pour produire les diagrammes si besoin.

Cadrage confirmé par l'utilisateur (2026-05-28) : **rester sur un module de la learning-path** (pas dans `docs/ressources/`). L'architecte décide INSERT / REPLACE / NEW-TRACK dans la taxonomie existante.

## Identité

- Slug : `pipeline-agents-handbook`
- Stage courant : `architect`
- Round : 0
- Créé : 2026-05-28

## Angle pédagogique pressenti

Module méta : « Comment ce handbook se construit avec des agents Copilot — guide contributeur + exemple vivant du workflow agentique ». Double objectif :

1. Permettre à un contributeur de comprendre et utiliser le pipeline (architect → writer → reviewer orchestré).
2. Servir d'étude de cas concrète du contenu du bloc "workflow" du handbook (sub-agents, gates, single-writer interlock, FRESH-CONTEXT review).

Genesis sera utilisé pour produire les diagrammes (séquence orchestrateur + sub-agents, boucle alignement bornée).

## Journal des étapes

- 2026-05-28 : plan créé, stage = architect, round = 0.
